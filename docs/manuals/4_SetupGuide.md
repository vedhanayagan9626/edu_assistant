# EduAssistant: Setup & Configuration Manual

Documentation for system administrators on deploying and configuring the EduAssistant environment.

---

## 📋 1. Prerequisites
Ensure the target environment meets the following requirements:
- **Python**: Version 3.10 or higher.
- **Node.js**: Version 18.0 or higher.
- **Environment**: Linux, macOS, or Windows 10/11.
- **Connectivity**: Outbound access to Google Gemini API endpoints.

## 🚀 2. Backend Deployment

1. **Clone & Navigate**:
   ```bash
   cd /backend
   ```
2. **Virtual Environment**:
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
   ```
3. **Install Dependencies**:
   ```bash
   pip install -r requirements.txt
   ```
4. **Environment Configuration**: Create a `.env` file in the backend root:
   ```env
   SECRET_KEY=your_secure_secret_key
   GEMINI_API_KEY=your_google_gemini_api_key
   SQLALCHEMY_DATABASE_URI=sqlite:///edu_assistant.db  # Or PostgreSQL URI
   ```
5. **Initialize Database**:
   ```bash
   python init_db.py  # If provided, or use flask shell to create_all
   ```
6. **Execute**:
   ```bash
   python run.py
   ```

## 💻 3. Frontend Deployment

1. **Navigate**:
   ```bash
   cd /frontend
   ```
2. **Install Packages**:
   ```bash
   npm install
   ```
3. **Environment Configuration**: Ensure `src/api/axios.js` points to the correct backend URL.
4. **Run Development Mode**:
   ```bash
   npm run dev
   ```
5. **Production Build**:
   ```bash
   npm run build
   ```

## 🧪 4. Post-Installation Verification
- Navigate to `http://localhost:5173`.
- Authenticate using the default Admin account (or use provided seed scripts).
- Create a test Department and User to verify access control.
- Upload a dummy PDF to verify RAG processing via ChromaDB.

---
*Generated by EduAssistant Documentation Engine*
